@model MiniMarket.Models.Product

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <title>Редактировать товар</title>
    <link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
    <div class="container py-4">
        <div class="row">

            <!-- Форма редактирования -->
            <div class="col-md-6 mb-4">
                <h2 class="mb-3">Редактировать товар</h2>

                <form asp-controller="Product" asp-action="EditProduct" method="post" class="p-3 border rounded bg-white shadow-sm">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="Id" />

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-bold"></label>
                        <input asp-for="Name" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="Price" class="form-label fw-bold"></label>
                        <input asp-for="Price" class="form-control" />
                    </div>

                    <button type="submit" class="btn btn-success">Сохранить изменения</button>
                </form>
            </div>

            <!-- Блок с советами -->
            <div class="col-md-6">
                <div class="card p-3 bg-white shadow-sm">
                    <h4 class="mb-3">Советы по заполнению</h4>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            Название товара должно быть <strong>кратким и понятным</strong>.
                        </li>
                        <li class="list-group-item">
                            Цена указывается <strong>только в цифрах</strong>, без лишних знаков.
                        </li>
                        <li class="list-group-item">
                            Проверь, что все обязательные поля <strong>заполнены</strong>.
                        </li>
                        <li class="list-group-item">
                            Избегай опечаток — это поможет покупателям лучше понять товар.
                        </li>
                    </ul>
                </div>
            </div>

        </div>
    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
